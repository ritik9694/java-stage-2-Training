package com.cognizant.java.lambda;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class FunctionTest {

	public static void main(String[] args) {
		/*
		 * String str = "Java";
		 * 
		 * Function<String, String> strLenFun = st -> st.toUpperCase();
		 * System.out.println(strLenFun.apply(str));
		 */
		
		/*
		 * List<String> names = Arrays.asList("Sanjay", "Praveen","Aravind", "Bharath",
		 * "Wilson", "Charles"); List<String> namesData = names.stream() .map(name ->
		 * String.format("%-20s --- %d",name.toUpperCase(), name.length()))
		 * .collect(Collectors.toList()); namesData.forEach(name ->
		 * System.out.println(name));
		 */
		
		
	}

}
